## This file must be regenerated by the script "sh/build_variant.sh" with your application configuration.
## For example: `sh/build_variant.sh build_res/afterlogic/build_variant.yaml`
## See [BUILD_INSTRUCTIONS.md] for more information.

name: aurorafiles
description: Temp configuration file
publish_to: none
version: 1.0.0

environment:
  sdk: '>=2.18.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.0
  build_variant:
    git: https://github.com/afterlogic/build_variant
